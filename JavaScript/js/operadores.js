// + - * / = ++ -- =+ -= || &&  == === != !== <= >=

var a,b,c;
a=3;
b=4;
total = a*b;

var idade,eleitor;
idade = 25;

//Condicional ternária
eleitor  = (idade < 18) ? "Não eleitor" : " Sim, eleitor"


console.log(eleitor);